version: 0.2

phases:
  build:
    commands:
      - echo "Build started at $(date)"
      - mkdir output
      - cp -r "AWS PROJECT/project-09/portfolio-repo/"* output/
      - echo "Files copied to output directory"
      - ls -la output

artifacts:
  base-directory: output
  files:
    - '**/*'
